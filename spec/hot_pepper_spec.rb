# frozen_string_literal: true

RSpec.describe HotPepper do
  it "has a version number" do
    expect(HotPepper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
